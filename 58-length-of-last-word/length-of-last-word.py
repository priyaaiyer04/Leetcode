class Solution(object):
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """
        a=list(s.split(" "))
        for i in a:
            if len(i)!=0:
                x=len(i)
        return x

        